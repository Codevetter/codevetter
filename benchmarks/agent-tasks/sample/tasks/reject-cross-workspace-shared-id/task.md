# Reject a shared identifier from another workspace

Update shared-document access so a matching shared identifier grants access
only within the actor's workspace. Preserve same-workspace sharing and unknown
identifier denial.
