## 1. React Evidence Authority

- [x] 1.1 Derive bounded per-component self-render duration from inclusive duration minus direct child duration without retaining Fiber graphs or application values.
- [x] 1.2 Advance the React document and aggregate evidence contract while keeping legacy captures readable and self-duration-ineligible.
- [x] 1.3 Record source-attribution scan completeness and withhold unique ownership when the bounded scan is partial.

## 2. Finding Contract and Detector

- [x] 2.1 Add the React commit-hotspot finding kind, detector identity, and fixed repetition, self-duration, and duration-share policy values to the closed performance-finding contract.
- [x] 2.2 Implement deterministic one-candidate React diagnosis from compatible `browser_react` evidence with complete unique repository source ownership.
- [x] 2.3 Emit explicit unavailable, insufficient-evidence, and ran coverage for every React evidence state without a redundancy, causality, or exact-exclusive-CPU claim.

## 3. Existing Surface Integration

- [x] 3.1 Include the detector in ordinary Playwright tool diagnosis, compact capture finding inventories, and autonomous performance-lab candidate selection without adding a public operation.
- [x] 3.2 Add the React detector to the closed detector-coverage matrix and preserve authoritative browser timing plus existing paired verification gates.

## 4. Deterministic Verification

- [x] 4.1 Add self-duration derivation and legacy-compatibility tests, including non-negative clamping and bounded traversal.
- [x] 4.2 Add positive, deterministic tie-break, exact-threshold, presentation-truncation, and stable finding-identity tests using closed React evidence fixtures.
- [x] 4.3 Add fail-closed tests for absent, unavailable, unprofiled, immaterial, external, ambiguous-source, and partial-source-scan evidence.
- [x] 4.4 Prove the finding contract separates observed commit activity and derived self duration from unverified redundancy, exact exclusive CPU, user impact, and production frequency.

## 5. Product Proof and Repository Gates

- [x] 5.1 Replay one locally runnable chosen React product flow and record either a source-linked hotspot or the exact no-finding/no-confidence boundary without editing the product first.
- [x] 5.2 Update the performance proof and canonical development documentation with evidence, limitations, and the unchanged 19-tool inventory.
- [x] 5.3 Run focused tests, the serialized runtime-failure-capsule suite, Biome, documentation validation, JSON artifact validation, and strict OpenSpec validation.
