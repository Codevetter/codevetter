## 1. Performance evidence foundation

- [x] 1.1 Add the versioned Runtime Performance Capsule contract, bounded sample policy, validation, and stable outcome states
- [x] 1.2 Parse and merge repository-owned V8 CPU profiles and Go benchmark measurements with explicit harness/application labels
- [x] 1.3 Implement compatible baseline comparison with recorded relative and absolute materiality thresholds

## 2. Closed profiling operation

- [x] 2.1 Extend the contained process runner with bounded Node/Vitest CPU profiling and an exact Go benchmark adapter
- [x] 2.2 Add warmup, independent timing samples, diagnostic profiling, temporary-artifact cleanup, and no-confidence failure handling
- [x] 2.3 Expose `profile` through the machine JSON CLI and root package scripts without changing existing operation outcomes

## 3. Qualification

- [x] 3.1 Add hermetic tests for distributions, V8 hotspots, Go measurements, redaction, bounds, comparisons, containment, and failed workloads
- [x] 3.2 Run read-only App Health Node and Go benchmark trials and capture measured coverage and limitations
- [x] 3.3 Run focused tests, lint, documentation validation, strict OpenSpec validation, and diff checks

## 4. Cross-product flywheel hardening

- [x] 4.1 Include untracked files in dirty snapshot identity without fabricating changed-line evidence
- [x] 4.2 Label Vitest scopes dominated by runner startup and use a bounded single-fork diagnostic profile pass
- [x] 4.3 Re-run consumer-product qualification and record whether Vitest application frames become attributable

## 5. Power-law product lanes

- [x] 5.1 Normalize bounded repository-owned Go CPU and allocation profile rows through `go tool pprof`
- [x] 5.2 Re-run App Health middleware qualification and identify observed allocation sites without editing App Health
- [x] 5.3 Run a deterministic Anime List recommendation benchmark at 1k, 10k, and 35k catalogue sizes without retaining changes in the product worktree
- [x] 5.4 Run a bounded Significant Hobbies browser trace against a loopback production build, or record the exact connector blocker without substituting hosted traffic
- [x] 5.5 Record cross-product findings, focused tests, docs validation, strict OpenSpec validation, and cleanup status

## 6. Agent-facing diagnosis loop

- [x] 6.1 Add the versioned deterministic performance-diagnosis contract and validator
- [x] 6.2 Rank capsule observations and classify regression, allocation, CPU, scaling, startup, and insufficient-evidence states
- [x] 6.3 Emit evidence-linked inferences, falsifiable hypotheses, one next bounded action, and a same-scope verification recipe
- [x] 6.4 Expose `diagnose-performance` through the machine JSON CLI and root package script
- [x] 6.5 Add hermetic tests and qualify the diagnosis on local App Health and Anime List workloads
- [x] 6.6 Run focused tests, lint, docs validation, strict OpenSpec validation, and diff checks

## 7. Source-aware optimization loop

- [x] 7.1 Collect redacted bounded source windows only for repository-owned runtime candidates
- [x] 7.2 Detect the first TypeScript/JavaScript sort, slice, eager-materialization, and repeated-pass pattern family
- [x] 7.3 Link source-pattern observations to diagnosis inferences and falsifiable optimization hypotheses
- [x] 7.4 Add a versioned domain-aware optimization-verification contract and pure comparator
- [x] 7.5 Expose `verify-optimization` through the closed CLI with contained baseline loading
- [x] 7.6 Add hermetic tests and qualify source-aware diagnosis against Anime List without retaining target changes
- [x] 7.7 Run focused tests, lint, docs validation, strict OpenSpec validation, and diff checks

## 8. Independent consumer-product confirmation

- [x] 8.1 Follow a startup-dominated Significant Hobbies result into a temporary representative scale workload
- [x] 8.2 Correct transformed TypeScript function anchoring and add repeated-traversal and nested-lookup source patterns
- [x] 8.3 Prevent single-pass benchmark noise from confirming an optimization by medianing repeated unprofiled metric runs
- [x] 8.4 Falsify the first candidate, identify the nested catalog lookup, and confirm the indexed lookup against the identical workload
- [x] 8.5 Remove temporary target artifacts, preserve the confirmed product change, and run focused validation

## 9. Paired and open-source qualification

- [x] 9.1 Add an opt-in contained paired verifier that alternates baseline and candidate execution order
- [x] 9.2 Require repeated matching domain metrics and fail closed for incomplete paired workloads
- [x] 9.3 Add hermetic paired-verification coverage and document the agent-facing operation
- [x] 9.4 Run blind local qualifications against immutable go-chi/chi and Marked revisions and follow only runtime-selected evidence
- [x] 9.4a Correct TypeScript class-method source bounds and add the runtime-selected split-prefix and key-membership patterns exposed by blind open-source qualification
- [x] 9.5 Close the pending Anime List and App Health hypotheses with verified before/after evidence or explicit inconclusive results
- [x] 9.6 Record accuracy outcomes, remove owned temporary artifacts, and run focused validation

## 10. CodeVetter self-improvement qualification

- [x] 10.1 Add a deterministic scale workload around an existing CodeVetter runtime-evidence operation
- [x] 10.2 Capture the unmodified product path through CodeVetter and retain the selected runtime evidence
- [x] 10.3 Apply one evidence-supported product optimization and verify the identical workload
- [x] 10.4 Record the observed improvement, rejected hypotheses, limitations, and scoped validation

## 11. External robustness and representative self-check

- [x] 11.1 Recheck the source-offset improvement against coverage emitted by the real CodeVetter runtime suite
- [x] 11.2 Profile a dependency-light public Node and Go corpus through exact local workloads
- [x] 11.3 Apply and identically verify only candidates supported by runtime evidence and correctness checks
- [x] 11.4 Record confirmed improvements, falsified hypotheses, accuracy gaps, resource bounds, and cleanup
