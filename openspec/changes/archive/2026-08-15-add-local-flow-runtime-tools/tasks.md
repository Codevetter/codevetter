## 1. Flow Contract And Capture

- [x] 1.1 Add and validate the bounded `runtime-flow-capsule/v1` contract with root, child, relationship, coverage, and limitation fields
- [x] 1.2 Add a Node diagnostic preload that captures redacted loopback HTTP client and server observations into owned temporary artifacts
- [x] 1.3 Run the flow pass separately from measurement and profiling, normalize its observations, and remove all owned raw artifacts

## 2. Trustworthy Diagnosis And Verification

- [x] 2.1 Normalize two independent diagnostic profile passes and record candidate repeatability and materiality policy
- [x] 2.2 Return `no_confidence` for unstable or immaterial V8 candidates while preserving independently supported deterministic domain-metric hypotheses
- [x] 2.3 Extend optimization verification with explicit mechanical, material, and shipping decisions without overstating low-sample statistics

## 3. Agent-Facing Product Tools

- [x] 3.1 Add a transport-independent bounded in-memory flow service for capture, inspect, explain, and verify operations
- [x] 3.2 Add a dependency-free repository-scoped MCP transport with closed tool schemas and sanitized errors
- [x] 3.3 Add the local runtime MCP package entrypoint and fix the documented package-script argument contract

## 4. Verification And Qualification

- [x] 4.1 Add focused unit and integration tests for flow normalization, HTTP redaction, cleanup, actionability, MCP schemas, identifiers, and comparisons
- [x] 4.2 Run the smallest runtime test suite, lint touched files, strict OpenSpec validation, and documentation validation
- [x] 4.3 Re-run the tool-only local HTTP qualification and one existing non-HTTP target, recording observed results and residual limitations

## 5. Request-Scoped Database Depth

- [x] 5.1 Reserve HTTP server events before handler execution and propagate their identifiers with `AsyncLocalStorage`
- [x] 5.2 Capture bounded request-scoped `node:sqlite` executions with normalized value-free SQL shapes and no arguments or rows
- [x] 5.3 Nest database flows beneath their request, compute interval-union accounted/unaccounted time, and expose operation summaries
- [x] 5.4 Add focused correlation, privacy, accounting, cleanup, and MCP inspection tests
- [x] 5.5 Requalify Foundry through the silent package entrypoint and record whether database evidence exposes a material bottleneck
- [x] 5.6 Run the complete runtime suite, lint, strict OpenSpec validation, docs validation, and package smoke

## 6. Deterministic Application Frequency

- [x] 6.1 Add an owned separate V8 coverage pass for Node and Vitest flow capture
- [x] 6.2 Normalize bounded repository-contained named function counts and original source anchors
- [x] 6.3 Join repeated function frequency with CPU candidates while preserving frequency-only and no-confidence outcomes
- [x] 6.4 Expose function-frequency analysis through capture and explain responses with cleanup metadata
- [x] 6.5 Add focused containment, privacy, call-count, evidence-join, and cleanup tests
- [x] 6.6 Requalify Foundry and record whether CodeVetter itself identifies repeated application work
- [x] 6.7 Run the full runtime suite, lint, docs, strict OpenSpec validation, diff checks, and package smoke

## 7. Cross-Project Vitest Accuracy

- [x] 7.1 Match a nested Vitest leaf name as an escaped full-name suffix and require exactly one executed assertion
- [x] 7.2 Capture runner-native V8 JSON into an owned directory with diagnostic thresholds disabled
- [x] 7.3 Normalize positive named functions from original TypeScript coverage anchors
- [x] 7.4 Keep unavailable optional function coverage from invalidating otherwise complete evidence
- [x] 7.5 Add focused selection, containment, normalization, and cleanup tests
- [x] 7.6 Requalify Significant Hobbies without inspecting or changing its product source
- [x] 7.7 Re-run the complete validation matrix
