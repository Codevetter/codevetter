## 1. Contracts and discovery

- [x] 1.1 Add closed versioned repository and portfolio qualification contracts with bounded statuses, candidates, evidence, and safety flags.
- [x] 1.2 Implement bounded package-scoped discovery for Node/Vitest test files and exact Go benchmark declarations.
- [x] 1.3 Rank candidates deterministically and classify conservative readiness with a safe next action.
- [x] 1.4 Add a closed standalone Node script profile adapter for exact local benchmark files.

## 2. Agent surfaces

- [x] 2.1 Add `qualify` and `qualify-portfolio` JSON CLI operations without changing existing runtime operations.
- [x] 2.2 Expose read-only repository qualification through the local runtime MCP server with a closed schema.
- [x] 2.3 Add a root package script and concise operator documentation for the new planning operation.

## 3. Verification and portfolio trial

- [x] 3.1 Test exact candidate parsing, score explanations, unsafe-signal handling, nested package scope, redaction, and every repository status.
- [x] 3.2 Test bounded mixed-portfolio behavior, ordering, continuation, no absolute path leakage, and invalid-manifest fail-closed behavior.
- [x] 3.3 Retain bounded redacted failure and exact workload-selection evidence for incomplete performance runs.
- [x] 3.4 Qualify every maintained Fleet application sequentially without executing project code and record the complete support matrix.
- [x] 3.5 Run selected explicit local performance workloads only where qualification evidence is strong, and record findings or evidence gaps without source edits.
- [x] 3.6 Run focused runtime tests, lint, documentation validation, strict OpenSpec validation, and diff checks.
