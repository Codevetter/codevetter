## 1. Contracts and fixtures

- [x] 1.1 Add strict versioned contracts for a selected-flow dossier, dependency observation, cause group, experiment queue, loop state, and terminal report
- [x] 1.2 Bound evidence families, queue size, allowed files, artifact bytes, experiment count, elapsed time, failures, and text fields
- [x] 1.3 Add a hermetic Vite/React fixture whose broad manual chunk predicate misclassifies an unrelated initial-route package
- [x] 1.4 Prove malformed, escaping, secret-like, oversized, stale, and unsupported inputs fail closed

## 2. Initial-route dependency attribution

- [x] 2.1 Build a bounded literal import and lazy-import graph rooted at one qualified Playwright route
- [x] 2.2 Resolve repository modules, package names, and package-manager real paths without evaluating application or configuration code
- [x] 2.3 Join the static closure with captured initial resources and repository module routes, retaining completeness and provenance
- [x] 2.4 Add optional contained build-artifact hashing and HTML/static-chunk closure with explicit verified or unverified provenance
- [x] 2.5 Recognize the closed Vite `manualChunks` predicate subset and report matching dependency paths, returned chunk labels, and source locations
- [x] 2.6 Return unsupported config syntax as a coverage gap and prove the analyzer never imports the Vite config or invokes a package script
- [x] 2.7 Traverse bounded deferred route modules, preserve initial-versus-deferred package reachability, and respect first-return chunk-rule order

## 3. Flow dossier and experiment planning

- [x] 3.1 Compose browser timing, loading, memory, React, action, and dependency evidence for one exact selected flow before yielding an edit
- [x] 3.2 Normalize observations into stable cause groups and keep observed facts separate from inferred hypotheses
- [x] 3.3 Produce deterministic bounded experiments with source boundaries, predicted metrics, correctness scope, verifier, rejection rule, and limitations
- [x] 3.4 Deduplicate cross-detector causes and rank by evidence quality, measured route bytes or runtime share, attribution uniqueness, edit boundary, and verification cost
- [x] 3.5 Persist the queue append-only with campaign, incumbent snapshot, flow, evidence, and planner identities
- [x] 3.6 Reuse review evidence selection to attach diff-relevant accepted evidence and repository-owned correctness scope without overriding runtime ranking

## 4. Autonomous agent loop

- [x] 4.1 Add service operations to plan a browser optimization loop, return the next untried experiment, and evaluate the current candidate
- [x] 4.2 Compose candidate evaluation with existing correctness-first screening and paired promotion without accepting commands or patches
- [x] 4.3 Reject protected or out-of-bound changes before execution and require exact incumbent restoration after rejection
- [x] 4.4 Advance the incumbent only after confirmed promotion, invalidate stale entries, and replan the selected flow
- [x] 4.5 Stop deterministically on exhaustion, plateau, budget, repeated failure, operational failure, or host blockage and emit a coverage-qualified report
- [x] 4.6 Expose the same closed protocol through repository CLI and MCP with compact machine-readable responses
- [x] 4.7 Update the autonomous agent program so a connected coding agent can run the loop without manually reconstructing campaign state
- [x] 4.8 Accept source-attested initial-route artifact movement as a primary metric only when paired browser evidence is stable and non-regressing

## 5. Verification and product trial

- [x] 5.1 Add unit and integration tests for dependency attribution, ranking, deduplication, append-only state, boundary enforcement, rejection, keep, replan, and resume
- [x] 5.2 Run the smallest touched runtime-capsule tests, lint, docs validation, strict OpenSpec validation, and diff checks
- [x] 5.3 Replay the planner on Anime List and prove the chunk-rule experiment ranks ahead of the lower-impact recommendation allocation hotspot
- [x] 5.4 Run at least one rejected and one retained Anime List experiment through the loop and compare first-pass evidence coverage, agent steps, local compute, and verified result with the prior manual investigation
- [x] 5.5 Document exact supported coverage, safety boundaries, failure modes, and the remaining gap between local evidence and production impact

> 5.4 completed on an isolated Anime checkout. The loop rejected a five-file
> Radix candidate at -9.254% below its transfer floor, then retained a two-file
> initial-flow Lucide boundary after three-sample screening and ten-sample paired
> promotion (-35.839% local navigation response bytes). A separate production
> build check measured only -0.313% initial gzip, so the result is explicitly a
> local-development win rather than claimed consumer impact. The durable proof
> compares evidence coverage, host steps, local elapsed time, and the prior
> manual investigation.
