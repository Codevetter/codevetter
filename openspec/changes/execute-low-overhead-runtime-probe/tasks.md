## 1. Derived Probe Inspection

- [x] 1.1 Accept the lower-overhead probe only when an integrity-checked runtime result recomputes the observer-effect route
- [x] 1.2 Project the exact source flow, required observation, and null source/edit authority through CLI and MCP
- [x] 1.3 Reject mismatched, stale, legacy-unavailable, incomplete, and caller-configured probes before execution

## 2. Profiler-Disabled Runtime

- [x] 2.1 Add a closed owned-runtime diagnostic profile that disables main-thread and Worker sampling profilers
- [x] 2.2 Preserve stream, process/thread CPU, response, async, framework, native, and local-network evidence under the new profile
- [x] 2.3 Require owned runtime attestation and reject unowned listener reuse for the new profile
- [x] 2.4 Prove the standard runtime profile and all existing captures remain unchanged

## 3. Corroboration and Routing

- [x] 3.1 Add a closed low-overhead corroboration contract with profiler state, exact CPU, native mechanisms, completeness, and provenance
- [x] 3.2 Route complete isolated GC, compilation, and libuv union activity at the fixed 5 ms floor
- [x] 3.3 Distinguish unresolved material CPU, immaterial CPU, incomplete evidence, failed correctness, and operational failure
- [x] 3.4 Persist probe-specific evidence separately from async inventory and Playwright correctness with legacy receipt readers

## 4. Repetition and Product Surfaces

- [x] 4.1 Derive stability routes from low-overhead corroboration and admit the probe in the three-observation scheduler
- [x] 4.2 Extend CLI/MCP schemas without accepting commands, paths, environment, base URL, network, concurrency, profiler, or mechanism values
- [x] 4.3 Cover unanimous, disagreement, unresolved, stale, correctness-failed, legacy, tampered, and incompatible repetitions

## 5. Product Proof

- [x] 5.1 Prove profiler-disablement, every mechanism route, threshold edges, raw-value exclusion, and non-causal authority on controlled fixtures
- [x] 5.2 Reload the existing High Signal runtime proof and schedules without execution or fabricated corroboration
- [x] 5.3 Execute one unchanged High Signal lower-overhead probe and record evidence, usefulness, cost, and limitations
- [x] 5.4 Document the product behavior and run focused tests, full runtime tests, lint, docs, diff, proof JSON, and strict OpenSpec validation

## 6. Owned Trace Sealing

- [x] 6.1 Split owned-runtime process sealing from private flow-directory cleanup with idempotent failure handling
- [x] 6.2 Defer server-flow collection until every browser diagnostic pass completes, then seal before parsing
- [x] 6.3 Preserve unowned-server behavior and require final artifact cleanup before accepting evidence
- [x] 6.4 Cover success, failed browser execution, seal failure, repeat cleanup, and existing runtime behavior
- [x] 6.5 Stream and interval-filter cold-runtime traces within fixed file, event, and per-event bounds
- [x] 6.6 Recapture the unchanged RolePatch lower-overhead flow and record whether native evidence closes
