## 1. Reconcile active product contracts

- [x] 1.1 Reconcile `make-verification-obvious` navigation and Home-spotlight expectations with the five-surface direction; preserve valid Review/Testing language and explicitly supersede Work/Board assumptions
- [x] 1.2 Add focused route and navigation tests that freeze Usage, Repo Unpack, Review, Testing, Performance, and Settings plus `/agents` and `/board` redirects
- [x] 1.3 Create a preserve-lane design receipt and capture the current shell before implementation

## 2. Closed desktop performance bridge

- [x] 2.1 Define shared typed request, progress, cancellation, outcome, and receipt contracts for plan, profile/diagnose, inspect, and paired verification operations
- [x] 2.2 Implement contained development and packaged-resource resolution for the existing runtime performance entry point without accepting arbitrary commands or adding a production dependency
- [x] 2.3 Add Tauri commands that validate closed inputs, qualify the required local runtime, supervise one owned process, sanitize bounded progress, parse one versioned JSON result, and preserve cleanup state
- [x] 2.4 Expose typed browser-guarded IPC wrappers and deterministic fixtures for success, unsupported runtime, unsafe scope, cancellation, timeout, malformed output, and cleanup failure
- [x] 2.5 Prove representative desktop bridge receipts are contract-equivalent to direct CLI/MCP results for the same exact scope

## 3. First-class Performance workbench

- [x] 3.1 Add a persistent `/performance` page using the existing project workspace to select a repository, adapter, relative target, exact workload, and bounded sample policy
- [x] 3.2 Render a pre-execution plan with repository identity, dirty state, zero-egress/cost posture, process count, samples, timeout, exclusions, and an explicit run action
- [x] 3.3 Render active and completed runs with cancellation, terminal status, observed latency/memory/allocation/bundle evidence, hotspot coverage, limitations, deterministic findings, hypotheses, and one next action
- [x] 3.4 Render campaign baseline, candidate ledger, promotion/challenge state, deterministic stop reason, and external-agent handoff without adding chat, task, terminal, or source-editing modes
- [x] 3.5 Add receipt copy/export and before/after proof presentation using existing machine-readable identities and verdicts
- [x] 3.6 Implement one shared scope resolver for function/flow language, pull request/change identity, and whole-codebase discovery across Testing and Performance; require explicit resolved-plan confirmation and uncovered-area reporting

## 4. Product-surface contraction

- [x] 4.1 Replace Work and Board navigation/shortcuts/command entries with Performance while preserving the existing shell geometry, visual tokens, focus behavior, and retained-route persistence
- [x] 4.2 Redirect `/agents` and `/board` to Usage and remove AgentPanel from the persistent product route registry without deleting local records or backend lifecycle code
- [x] 4.3 Update onboarding, keyboard help, product-surface documentation, and desktop tests to describe review, executable testing, and performance improvement rather than embedded building or task orchestration
- [x] 4.4 Audit the resulting dependency graph, remove proven-orphaned Work/Board presentation modules, and explicitly retain backend/data cleanup behind a separate export-and-migration decision
- [x] 4.5 Remove visible per-destination shortcut badges and the custom global `g` navigation chords while preserving command search, focus, and ordinary route activation

## 5. Qualification and handoff

- [x] 5.1 Run focused bridge/unit tests, desktop typecheck, lint, production build, route tests, and `git diff --check`; fix only regressions caused by this change
- [x] 5.2 Qualify empty, blocked, planned, running, failed, no-confidence, diagnosed, and paired-proof Performance states plus retained and redirected routes in the Tauri app
- [x] 5.3 Capture 390, 768, and 1440 evidence, run Impeccable critique/polish/audit, resolve all P0/P1 findings, and obtain owner `keep` or explicit delegated feedback
- [x] 5.4 Update `docs/product/surfaces.md` and `PROJECT_STATUS.md` only with implemented verified outcomes; do not claim autonomous optimization or product impact from an unrun campaign
- [ ] 5.5 Validate the OpenSpec change strictly and archive it only after implementation, receipt equivalence, and shipping handoff are complete
