## 1. Runtime Mechanism Evidence

- [x] 1.1 Add closed whole-request and pre-commit runtime-mechanism contracts with legacy CPU compatibility
- [x] 1.2 Classify every admitted Node/V8 mechanism without exposing raw URL, function, ID, version, or source values
- [x] 1.3 Preserve complete timing/sample accounting and fail closed for missing nodes, deltas, overlap, and boundaries
- [x] 1.4 Add fixed dominance and observer-effect routing with low-confidence non-causal authority

## 2. Browser Probe Execution

- [x] 2.1 Evolve browser server-flow and Playwright diagnosis output while retaining legacy capture validation
- [x] 2.2 Project runtime mechanisms and a bounded next observation through `inspect-browser-probe`
- [x] 2.3 Execute `inspect_main_thread_runtime` through CLI/MCP recapture on the same exact qualified flow
- [x] 2.4 Persist probe-specific runtime inventory separately from correctness and reject arbitrary execution values

## 3. Repetition and Safety

- [x] 3.1 Derive stability routes from runtime mechanisms rather than the unchanged outer probe
- [x] 3.2 Admit runtime probes in the three-observation scheduler without weakening inventory-probe behavior
- [x] 3.3 Cover unanimous, disagreement, unresolved, failed-correctness, stale, legacy, tampered, and incompatible cases
- [x] 3.4 Prove raw runtime identities and profiler observer frames cannot become source or edit authority

## 4. Product Proof

- [x] 4.1 Prove every closed mechanism and threshold edge on controlled request profiles
- [x] 4.2 Reload the existing High Signal legacy inspection, stability assessment, and schedule unchanged
- [x] 4.3 Execute one unchanged High Signal runtime probe and record observed mechanism evidence and limitations
- [x] 4.4 Document the new tool behavior, local cost, legacy boundary, findings, and remaining executable probes
- [x] 4.5 Run focused tests, full runtime tests, lint, docs validation, diff checks, and strict OpenSpec validation

## 5. Isolated Pre-Commit Recovery

- [x] 5.1 Evolve private raw-profile and normalized CPU contracts with backward-compatible pre-commit overlap accounting
- [x] 5.2 Record whether each overlapping dynamic request begins before or after the profiled response commits
- [x] 5.3 Retain only an isolated pre-commit mechanism view while keeping the whole-request profile contaminated and source-ineligible
- [x] 5.4 Cover post-commit redirect overlap, true pre-commit overlap, malformed counts, legacy profiles, and authority invariants
- [x] 5.5 Recapture the unchanged RolePatch flow, preserve bounded proof, document limitations, and run full verification
