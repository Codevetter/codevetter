## 1. Closed static parsing

- [x] 1.1 Generalize the existing immutable environment-fallback terminal to retain one bounded quoted fallback without reading the environment
- [x] 1.2 Preserve numeric port-template validation and strict loopback normalization in their consuming contexts
- [x] 1.3 Keep ambiguous, environment-only, remote, computed, property-derived, call-derived, and oversized aliases ineligible

## 2. Qualification verification

- [x] 2.1 Add a RolePatch-shaped config fixture with `baseURL` and server command constants and prove the loopback Next flow qualifies
- [x] 2.2 Prove a populated process environment cannot change the fallback-derived origin and executable config code is not run
- [x] 2.3 Add negative cases and rerun existing literal, port-template, project-profile, and server-family tests

## 3. Real-product evidence

- [x] 3.1 Requalify unchanged current RolePatch and confirm the exact local browser candidates, snapshot identity, and owned Next family
- [x] 3.2 Run one bounded unchanged qualified RolePatch flow if the remaining owned-runtime safety gates permit it
- [x] 3.3 Record correctness, runtime state, wall time, memory, diagnosis, retained evidence, and any explicit terminal boundary without production claims
- [x] 3.4 Continue through the existing browser probe chain only when the emitted route and correctness authorize it

## 4. Compatibility and documentation

- [x] 4.1 Document the newly admitted config grammar and its non-evaluation boundary
- [x] 4.2 Run focused qualification and runtime tests, full runtime tests, lint, docs validation, diff checks, and strict OpenSpec validation
- [x] 4.3 Record durable proof JSON that separates observed evidence, inference, and unverified production impact
