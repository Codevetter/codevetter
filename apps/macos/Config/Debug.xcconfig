// Debug.xcconfig
// Debug configuration for macOS projects - minimal overrides only
// Generated by XcodeBuildMCP

#include "Shared.xcconfig"

// Development must exercise the real supervised Rust/Node/browser process
// boundary. Ad-hoc development code has no shared Team ID with Sparkle, so the
// Debug-only entitlement disables Library Validation. Release remains empty
// and production must sign every nested component with one Developer ID team.
CODE_SIGN_ENTITLEMENTS = Config/CodeVetter.Debug.entitlements
