/** Suggested questions users can ask against an unpack snapshot (optional AI step). */
export const UNPACK_ASK_PRESETS = [
  'What are the highest-risk areas to change?',
  'How does data persist in this repo?',
  'What should I read before making a UI change?',
  'What test coverage exists and what is missing?',
  'Where are the main extension points for new features?',
] as const;
