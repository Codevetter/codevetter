import type { EditorialContent } from '@/data/verification-content';

export const articlesContent: Record<string, EditorialContent> = {
  '/articles/agent-evaluation-vs-tests-vs-code-review': {
    title: 'Agent evaluation vs tests vs code review: what each can actually prove',
    seoTitle: 'Agent Eval vs Tests vs Code Review | What Each Proves',
    description:
      'Understand the distinct proof boundaries of agent evaluation, software testing, and static AI code review when checking agent-written software changes.',
    path: '/articles/agent-evaluation-vs-tests-vs-code-review',
    intent:
      'architectural comparison and strategy guide for technical leaders structuring quality systems for coding agents',
    schema: ['Article', 'BreadcrumbList'],
    internalLinks: [
      '/ai-code-review-vs-verification',
      '/coding-agent-verification',
      '/verify-ai-generated-code',
      '/verification-evidence-bundle',
      '/benchmark',
    ],
    markdown:
      '## Understanding Quality Gates in the Era of AI Coding Agents\n\nAs AI coding agents take on complex software tasks, technical leaders face a critical question: how do we know an agent-authored pull request is safe to merge?\n\nIn traditional software development, quality assurance relies on **automated test suites** (unit, integration, and end-to-end tests) and **human code review**. With generative AI, organizations have added **static AI code review**, where an LLM inspects diffs. More recently, **execution-backed agent evaluation** systems have emerged to assess whether an agent completed a requested task correctly.\n\nHowever, teams frequently blur the boundaries between these mechanisms, treating them as interchangeable. This leads to misplaced confidence—such as assuming a green test suite proves an agent understood the prompt, or believing an AI review comment proves a bug was fixed.\n\nTo build a reliable software delivery pipeline for AI-generated code, teams must understand what static code review, software testing, and execution-backed agent evaluation can—and cannot—actually prove.\n\n## The Core Comparison Matrix\n\nThe table below outlines the fundamental differences across all three mechanisms:\n\n| Dimension | Static AI / Human Code Review | Automated Testing (Unit / Integration / E2E) | Execution-Backed Agent Evaluation |\n| --- | --- | --- | --- |\n| **Primary Input** | Code diff, repository files, style rules | Source code, test scripts, test data | Task intent, repository revision, patch, executable checks |\n| **Primary Output** | Text findings, style suggestions, risk flags | Pass/fail counts, stack traces, coverage reports | Portable evidence receipt, failure taxonomy, completion verdict |\n| **What It Establishes** | Source-level findings and maintainability judgments | Whether specific assertions pass under defined inputs | Evidence for the acceptance criteria and regression checks actually exercised |\n| **Core Limitation** | Cannot execute code; plausible diffs can fail | Cannot determine if tests match prompt intent | Requires explicit executable checks; bounded by runner |\n| **Failure Mode** | False positives or false praise from text | Green suite passing on an incomplete task | Incomplete checks can still miss defects; missing evidence must remain explicit |\n\n## 1. Static Code Review: Evaluating Appearance and Maintainability\n\nStatic code review—performed by engineers or AI models—inspects source text. It evaluates structure, style, and architectural patterns without executing the application.\n\n### What Static Code Review Can Prove\n- **Conformity to Style and Conventions:** Verifies that variable naming, organization, and formatting match repository standards.\n- **Surface Risk Identification:** Flags known anti-patterns, such as hardcoded credentials or missing error-handling blocks.\n- **Maintainability and Readability:** Evaluates whether code is modular and understandable for future maintainers.\n\n### What Static Code Review Cannot Prove\n- **Operational Correctness:** A diff can be beautifully formatted and completely broken at runtime due to an unhandled null pointer or async race condition.\n- **Task Completion:** A reviewer inspecting a diff cannot verify if an API endpoint handles real HTTP headers or whether a migration executes cleanly.\n\n## 2. Automated Testing: Verifying Specific Code Assertions\n\nSoftware testing executes code within a controlled runner to verify that specific functions satisfy predefined code assertions.\n\n### What Testing Can Prove\n- **Assertion Validity:** Proves that specified code paths yield expected return values under defined test inputs.\n- **Regression Detection for Covered Code:** Demonstrates that existing behaviors continue to function after new code is added.\n- **Performance Benchmarks:** Measures execution wall time, memory consumption, or CPU utilization for specific benchmark functions.\n\n### What Testing Cannot Prove\n- **Alignment with Task Intent:** A coding agent can satisfy test runners by altering test assertions or deleting failing tests entirely. The test suite passes, but the business requirement is unsatisfied.\n- **Correctness of Un-Tested Requirements:** If a task requires handling an edge case that lacks a written test, a passing test suite provides zero proof that the edge case works.\n\n## 3. Execution-Backed Agent Evaluation: Verifying Task Completion\n\nExecution-backed agent evaluation helps bridge the "intent-to-execution" gap in autonomous software development. It connects the prompt, the exact patch, isolated local execution, and structured evidence collection. Someone still has to establish that the checks represent the requested behavior.\n\n```\n[User Task Prompt] ──> [Agent Patch] ──> [Executable Verification] ──> [Evidence Receipt]\n                                                │\n                                                ├── Unit / Integration Tests\n                                                ├── Headless Browser Flows\n                                                └── Local API Assertions\n```\n\n### What Agent Evaluation Can Prove\n- **Covered Acceptance Criteria:** Records whether mapped checks pass at a behavioral boundary, such as headless browser state or API endpoints. Unchecked requirements remain unverified.\n- **Failure Classification:** Compares failure evidence with baseline runs and environment observations. A taxonomy label alone does not establish causation.\n- **Auditability and Re-Checks:** Can link an initial failure to a passing re-check when the runner records both attempts and their source identities. The link is bounded evidence, not a guarantee that every regression is resolved.\n\n### What Agent Evaluation Cannot Prove\n- **Universal Code Quality:** A passing evaluation covers its checks, inputs, environment, and revision. It cannot establish universal runtime correctness or architectural quality.\n- **Coverage Beyond Executable Checks:** Agent evaluation cannot verify behavior for which no executable check or browser automation can be constructed.\n\n## Why Relying on Any Single Mechanism Fails\n\nEngineering organizations relying on only one mechanism face predictable failure modes:\n\n1. **Code Review Alone:** Merges "plausible-looking code", leading to runtime outages, state corruption, or API breaks.\n2. **Tests Alone:** Results in "passing suites with drift," where agents satisfy test runners by altering assertions or ignoring un-tested edge cases.\n3. **Agent Evaluation Alone:** Can miss untested behavior and maintainability problems even when all selected checks pass.\n\n## Building a Unified Quality Pipeline for AI Code\n\nTechnical teams should integrate all three mechanisms into a unified, complementary quality loop:\n\n```\n                  ┌─────────────────────────────────────────┐\n                  │ 1. STATIC CODE REVIEW                   │\n                  │ Discover risks, check style & structure │\n                  └────────────────────┬────────────────────┘\n                                       │\n                                       ▼\n                  ┌─────────────────────────────────────────┐\n                  │ 2. EXECUTABLE AGENT EVALUATION          │\n                  │ Check mapped criteria & browser/API     │\n                  └────────────────────┬────────────────────┘\n                                       │\n                                       ▼\n                  ┌─────────────────────────────────────────┐\n                  │ 3. AUTOMATED REGRESSION SUITE           │\n                  │ Re-run tests to catch side effects      │\n                  └─────────────────────────────────────────┘\n```\n\n1. **Use Static Review for Triage:** Let static AI code review scan incoming diffs to identify structural risks and maintainability concerns.\n2. **Translate Acceptance Criteria into Executable Checks:** Ensure changes are exercised against real behavioral boundaries, such as Playwright browser flows or local API tests.\n3. **Generate Machine-Readable Evidence Receipts:** Capture execution telemetry, failure taxonomies, and resource bounds into portable JSON receipts.\n4. **Require Closure Trails Before Merging:** Require the agent to apply a fix and generate a passing re-check receipt linked to the original failure before approving the pull request.\n\n## Next Action for Engineering Leadership\n\nAudit your engineering team\'s current pull request process for AI-generated code. Establish an execution-backed evaluation layer that binds user prompt intent to isolated, executable checks and structured evidence receipts.\n\nTo inspect how local execution-backed verification functions in practice, download CodeVetter or review our public benchmark methodology.',
    html: '<h2>Understanding Quality Gates in the Era of AI Coding Agents</h2>\n<p>As AI coding agents take on complex software tasks, technical leaders face a critical question: how do we know an agent-authored pull request is safe to merge?</p>\n<p>In traditional software development, quality assurance relies on <strong>automated test suites</strong> (unit, integration, and end-to-end tests) and <strong>human code review</strong>. With generative AI, organizations have added <strong>static AI code review</strong>, where an LLM inspects diffs. More recently, <strong>execution-backed agent evaluation</strong> systems have emerged to assess whether an agent completed a requested task correctly.</p>\n<p>However, teams frequently blur the boundaries between these mechanisms, treating them as interchangeable. This leads to misplaced confidence—such as assuming a green test suite proves an agent understood the prompt, or believing an AI review comment proves a bug was fixed.</p>\n<p>To build a reliable software delivery pipeline for AI-generated code, teams must understand what static code review, software testing, and execution-backed agent evaluation can—and cannot—actually prove.</p>\n<h2>The Core Comparison Matrix</h2>\n<p>The table below outlines the fundamental differences across all three mechanisms:</p>\n<table>\n<thead>\n<tr>\n<th>Dimension</th>\n<th>Static AI / Human Code Review</th>\n<th>Automated Testing (Unit / Integration / E2E)</th>\n<th>Execution-Backed Agent Evaluation</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Primary Input</strong></td>\n<td>Code diff, repository files, style rules</td>\n<td>Source code, test scripts, test data</td>\n<td>Task intent, repository revision, patch, executable checks</td>\n</tr>\n<tr>\n<td><strong>Primary Output</strong></td>\n<td>Text findings, style suggestions, risk flags</td>\n<td>Pass/fail counts, stack traces, coverage reports</td>\n<td>Portable evidence receipt, failure taxonomy, completion verdict</td>\n</tr>\n<tr>\n<td><strong>What It Establishes</strong></td>\n<td>Source-level findings and maintainability judgments</td>\n<td>Whether specific assertions pass under defined inputs</td>\n<td>Evidence for the acceptance criteria and regression checks actually exercised</td>\n</tr>\n<tr>\n<td><strong>Core Limitation</strong></td>\n<td>Cannot execute code; plausible diffs can fail</td>\n<td>Cannot determine if tests match prompt intent</td>\n<td>Requires explicit executable checks; bounded by runner</td>\n</tr>\n<tr>\n<td><strong>Failure Mode</strong></td>\n<td>False positives or false praise from text</td>\n<td>Green suite passing on an incomplete task</td>\n<td>Incomplete checks can still miss defects; missing evidence must remain explicit</td>\n</tr>\n</tbody></table>\n<h2>1. Static Code Review: Evaluating Appearance and Maintainability</h2>\n<p>Static code review—performed by engineers or AI models—inspects source text. It evaluates structure, style, and architectural patterns without executing the application.</p>\n<h3>What Static Code Review Can Prove</h3>\n<ul>\n<li><strong>Conformity to Style and Conventions:</strong> Verifies that variable naming, organization, and formatting match repository standards.</li>\n<li><strong>Surface Risk Identification:</strong> Flags known anti-patterns, such as hardcoded credentials or missing error-handling blocks.</li>\n<li><strong>Maintainability and Readability:</strong> Evaluates whether code is modular and understandable for future maintainers.</li>\n</ul>\n<h3>What Static Code Review Cannot Prove</h3>\n<ul>\n<li><strong>Operational Correctness:</strong> A diff can be beautifully formatted and completely broken at runtime due to an unhandled null pointer or async race condition.</li>\n<li><strong>Task Completion:</strong> A reviewer inspecting a diff cannot verify if an API endpoint handles real HTTP headers or whether a migration executes cleanly.</li>\n</ul>\n<h2>2. Automated Testing: Verifying Specific Code Assertions</h2>\n<p>Software testing executes code within a controlled runner to verify that specific functions satisfy predefined code assertions.</p>\n<h3>What Testing Can Prove</h3>\n<ul>\n<li><strong>Assertion Validity:</strong> Proves that specified code paths yield expected return values under defined test inputs.</li>\n<li><strong>Regression Detection for Covered Code:</strong> Demonstrates that existing behaviors continue to function after new code is added.</li>\n<li><strong>Performance Benchmarks:</strong> Measures execution wall time, memory consumption, or CPU utilization for specific benchmark functions.</li>\n</ul>\n<h3>What Testing Cannot Prove</h3>\n<ul>\n<li><strong>Alignment with Task Intent:</strong> A coding agent can satisfy test runners by altering test assertions or deleting failing tests entirely. The test suite passes, but the business requirement is unsatisfied.</li>\n<li><strong>Correctness of Un-Tested Requirements:</strong> If a task requires handling an edge case that lacks a written test, a passing test suite provides zero proof that the edge case works.</li>\n</ul>\n<h2>3. Execution-Backed Agent Evaluation: Verifying Task Completion</h2>\n<p>Execution-backed agent evaluation helps bridge the &quot;intent-to-execution&quot; gap in autonomous software development. It connects the prompt, the exact patch, isolated local execution, and structured evidence collection. Someone still has to establish that the checks represent the requested behavior.</p>\n<pre><code>[User Task Prompt] ──&gt; [Agent Patch] ──&gt; [Executable Verification] ──&gt; [Evidence Receipt]\n                                                │\n                                                ├── Unit / Integration Tests\n                                                ├── Headless Browser Flows\n                                                └── Local API Assertions\n</code></pre>\n<h3>What Agent Evaluation Can Prove</h3>\n<ul>\n<li><strong>Covered Acceptance Criteria:</strong> Records whether mapped checks pass at a behavioral boundary, such as headless browser state or API endpoints. Unchecked requirements remain unverified.</li>\n<li><strong>Failure Classification:</strong> Compares failure evidence with baseline runs and environment observations. A taxonomy label alone does not establish causation.</li>\n<li><strong>Auditability and Re-Checks:</strong> Can link an initial failure to a passing re-check when the runner records both attempts and their source identities. The link is bounded evidence, not a guarantee that every regression is resolved.</li>\n</ul>\n<h3>What Agent Evaluation Cannot Prove</h3>\n<ul>\n<li><strong>Universal Code Quality:</strong> A passing evaluation covers its checks, inputs, environment, and revision. It cannot establish universal runtime correctness or architectural quality.</li>\n<li><strong>Coverage Beyond Executable Checks:</strong> Agent evaluation cannot verify behavior for which no executable check or browser automation can be constructed.</li>\n</ul>\n<h2>Why Relying on Any Single Mechanism Fails</h2>\n<p>Engineering organizations relying on only one mechanism face predictable failure modes:</p>\n<ol>\n<li><strong>Code Review Alone:</strong> Merges &quot;plausible-looking code&quot;, leading to runtime outages, state corruption, or API breaks.</li>\n<li><strong>Tests Alone:</strong> Results in &quot;passing suites with drift,&quot; where agents satisfy test runners by altering assertions or ignoring un-tested edge cases.</li>\n<li><strong>Agent Evaluation Alone:</strong> Can miss untested behavior and maintainability problems even when all selected checks pass.</li>\n</ol>\n<h2>Building a Unified Quality Pipeline for AI Code</h2>\n<p>Technical teams should integrate all three mechanisms into a unified, complementary quality loop:</p>\n<pre><code>                  ┌─────────────────────────────────────────┐\n                  │ 1. STATIC CODE REVIEW                   │\n                  │ Discover risks, check style &amp; structure │\n                  └────────────────────┬────────────────────┘\n                                       │\n                                       ▼\n                  ┌─────────────────────────────────────────┐\n                  │ 2. EXECUTABLE AGENT EVALUATION          │\n                  │ Check mapped criteria &amp; browser/API     │\n                  └────────────────────┬────────────────────┘\n                                       │\n                                       ▼\n                  ┌─────────────────────────────────────────┐\n                  │ 3. AUTOMATED REGRESSION SUITE           │\n                  │ Re-run tests to catch side effects      │\n                  └─────────────────────────────────────────┘\n</code></pre>\n<ol>\n<li><strong>Use Static Review for Triage:</strong> Let static AI code review scan incoming diffs to identify structural risks and maintainability concerns.</li>\n<li><strong>Translate Acceptance Criteria into Executable Checks:</strong> Ensure changes are exercised against real behavioral boundaries, such as Playwright browser flows or local API tests.</li>\n<li><strong>Generate Machine-Readable Evidence Receipts:</strong> Capture execution telemetry, failure taxonomies, and resource bounds into portable JSON receipts.</li>\n<li><strong>Require Closure Trails Before Merging:</strong> Require the agent to apply a fix and generate a passing re-check receipt linked to the original failure before approving the pull request.</li>\n</ol>\n<h2>Next Action for Engineering Leadership</h2>\n<p>Audit your engineering team&#39;s current pull request process for AI-generated code. Establish an execution-backed evaluation layer that binds user prompt intent to isolated, executable checks and structured evidence receipts.</p>\n<p>To inspect how local execution-backed verification functions in practice, download CodeVetter or review our public benchmark methodology.</p>',
  },
  '/articles/coding-agent-evidence-receipt-spec': {
    title: 'What an evidence receipt for a coding agent should contain',
    seoTitle: 'Coding Agent Evidence Receipt Spec | Verification Bundles',
    description:
      'Learn what a machine-readable evidence receipt for AI coding agents must contain, including identity bindings, execution records, taxonomy, and failure trails.',
    path: '/articles/coding-agent-evidence-receipt-spec',
    intent: 'architectural reference for engineers building auditable agent verification pipelines',
    schema: ['Article', 'BreadcrumbList'],
    internalLinks: [
      '/verification-evidence-bundle',
      '/coding-agent-verification',
      '/verify-ai-generated-code',
      '/ai-code-review-vs-verification',
      '/benchmark',
    ],
    markdown:
      "## Why Informal Agent Self-Reports Fail Auditability\n\nWhen an autonomous coding agent completes a task, it typically posts a pull request accompanied by an informal Markdown summary describing what it claims to have changed and tested.\n\nWhile readable, these text summaries are unsuited for technical auditability or automated verification. An agent self-report suffers from four fundamental flaws:\n\n1. **Non-Verifiable Claims:** An agent asserting that \"all tests passed\" provides no proof that tests were actually executed against the checked-in commit SHA.\n2. **Missing Execution Context:** Summaries rarely record the precise environment variables, node versions, database states, CLI flags, or mock settings used during execution.\n3. **Selective Omission:** A summary can omit unexecuted suites, skipped assertions, timeouts, or intermediate failures.\n4. **Lack of a Stable Contract:** Free-form summaries lack the schema needed for reliable automated validation.\n\nStructured, machine-readable **verification evidence receipts** make these claims easier to inspect. They complement source review and runner controls; the receipt format itself does not make an agent safe.\n\n## General Receipt-Design Principles vs. Current Implementations\n\nA verification evidence receipt is a versioned, portable artifact recording the evidence its producer collected, along with missing measurements and trust limitations.\n\nIn general receipt design, an evidence specification defines structured schemas that bind source code identities to execution telemetry. In practical software systems, these fall into two distinct categories:\n\n- **Experimental / Ingested Project Receipts:** Ingestion schemas (such as `codevetter.project-verification-receipt/v1`) that parse raw external runner outputs (Playwright JSON, JUnit XML, LCOV) without mutating local state or asserting full system authority.\n- **Persisted Native / Local Receipts:** Local execution receipts (such as `codevetter.local-check/v1`) emitted and stored in local databases during supervised verification passes.\n\nThe sections below propose a general design checklist, not a single shipped CodeVetter schema. The experimental project-receipt analyzer does not execute tests, discover commands, or persist receipts into the desktop database. It cannot supply measurements absent from its input. Hashes identify content; they do not authenticate the producer or make storage immutable.\n\n```\n┌─────────────────────────────────────────────────────────────────────────┐\n│              VERIFICATION EVIDENCE RECEIPT SCHEMA (v1)                  │\n├─────────────────────────────────────────────────────────────────────────┤\n│ 1. Identity & Scope    │ Base Commit, Patch SHA, Task ID, Runner Profile │\n│ 2. Execution Inventory │ Commands, Exit Codes, Output Hash, Duration    │\n│ 3. Failure Taxonomy    │ Agent Regressions vs. Environment vs. Pre-Exist│\n│ 4. Resource Telemetry  │ Peak RSS, Wall Time, CPU, Egress, Sandbox State │\n│ 5. Explicit Bounds     │ Unverified Requirements, Missing Test Paths     │\n│ 6. Fix Closure Trail   │ Linkage to Initial Failure Receipt & Re-check   │\n└─────────────────────────────────────────────────────────────────────────┘\n```\n\nBelow is a detailed breakdown of what each section of a robust evidence receipt specification must contain.\n\n## 1. Identity and Scope Bindings\n\nAn evidence receipt must be linked unambiguously to a specific source tree and task description. The receipt begins by pinning identity references:\n\n- **Repository & Revision Identity:** Absolute Git commit SHA of the base repository, target branch, and exact SHA-256 digest of the agent's patch file.\n- **Task Intent Reference:** A unique task identifier, along with a digest or text snapshot of the original prompt and acceptance criteria.\n- **Environment & System Profile:** OS kernel version, architecture (e.g., `darwin-arm64`), runtime engine version (e.g., Node.js, Rust), and installed CLI versions.\n- **Verifier Profile:** The specific verifier name, executable version, and schema version (e.g., `codevetter.local-check/v1`).\n\nChecking these identities against the candidate revision helps detect \"replay drift\"—where an older receipt is attached to a new pull request. Declared identity fields alone do not prove which code the producer executed.\n\n## 2. Execution Records and Inventory\n\nThe execution section records every command invoked during the verification pass, documenting exact inputs and outputs:\n\n- **Command Identity:** The selected command and safe arguments, with credential-bearing values redacted. Record omissions explicitly rather than copying secrets into a receipt.\n- **Terminal Exit States:** Numeric exit codes, process signal terminations (e.g., `SIGKILL`), and cancellation flags.\n- **Bounded Output Captures:** SHA-256 hashes of stdout and stderr logs, accompanied by bounded text excerpts (e.g., capped at 64 KiB) capturing the failure tail.\n- **Test Inventory Details:** Individual counts for total discovered tests, passed tests, failed tests, skipped tests, and suite durations.\n- **Stable Failure Signatures:** Normalized, hashed representations of error messages and stack traces, stripping non-deterministic elements like timestamps.\n\n## 3. Failure Taxonomy and Classification\n\nA simple pass/fail flag collapses important operational distinctions. A receipt can record the following classifications when baseline and runner evidence support them; otherwise attribution should remain uncertain:\n\n- **Agent-Introduced Defect:** The test failed due to a logical error or broken assertion directly within the code written by the agent.\n- **Side-Effect Regression:** A test outside the immediate scope of the requested task failed after the patch was applied, indicating unexpected breakage.\n- **Pre-Existing Failure:** The check failed identically on the base commit SHA before the agent's patch was applied.\n- **Operational / Environment Failure:** Execution was aborted due to infrastructure conditions, such as missing dependencies or port conflicts.\n- **Transient Recovery:** A test that failed initially but passed upon immediate re-execution, flagged for flaky behavior tracking.\n\n## 4. Resource, Egress, and Safety Boundaries\n\nExecuting arbitrary agent code presents security and resource risks. A receipt should describe the controls and observations available to assess those risks, without treating incomplete telemetry as proof of safety:\n\n- **Resource Consumption:** Peak process-tree Resident Set Size (RSS memory in MiB), sampled CPU usage, wall-time duration, and process spawn counts.\n- **Network Egress Evidence:** Observed requests or escapes, collection coverage, and the policy actually enforced. An absent network field is not evidence of zero egress; destination details should be minimized or redacted where sensitive.\n- **Sandbox State:** The configured isolation boundaries and evidence that enforcement was active. A producer's declaration alone is not independent attestation.\n\n## 5. Explicit Uncertainty and Limitation Bounds\n\nMissing evidence is not proof of success. If an agent modifies an API route that lacks test coverage, the evidence receipt must make that gap explicit.\n\nThe limitations section records:\n\n- **Unverified Acceptance Criteria:** User requirements that could not be mapped to an executable check.\n- **Unexecuted Code Paths:** Files modified by the agent that were not exercised by any executed test runner (supported by LCOV or Cobertura XML reports).\n- **`no_confidence` States:** Sections where telemetry collection was partial or where test reports lack revision-bound selection or resource measurements.\n\n## 6. Fix Linkage and Closure Trails\n\nWhen an agent run fails verification, a fix cycle begins. A production-grade evidence receipt specification supports receipt chaining to establish an auditable history of resolution:\n\n1. **Initial Failure Receipt:** Emitted when the agent's first patch fails an executable check, containing the failing signature and diff.\n2. **Fix Candidate Linkage:** The corrective agent run references the initial failure receipt by its unique SHA-256 bundle identity.\n3. **Targeted Re-Check Execution:** The new verification run re-executes the exact failing check identified in the initial receipt.\n4. **Re-Check Receipt:** Records that the targeted check passed at the new revision and links the retained attempts. Broader regression checks and incomplete coverage remain separate from that result.\n\n## Next Action for Platform Engineers\n\nIncorporate structured evidence receipts into your agent execution infrastructure. Replace informal Markdown summaries with a versioned, schema-validated JSON receipt specification that captures base commit identities, command inventory, failure taxonomy, resource bounds, and explicit limitation boundaries.\n\nTo inspect executable receipt schemas and CLI ingestion tooling, review CodeVetter's open verification documentation or download the local desktop viewer.",
    html: '<h2>Why Informal Agent Self-Reports Fail Auditability</h2>\n<p>When an autonomous coding agent completes a task, it typically posts a pull request accompanied by an informal Markdown summary describing what it claims to have changed and tested.</p>\n<p>While readable, these text summaries are unsuited for technical auditability or automated verification. An agent self-report suffers from four fundamental flaws:</p>\n<ol>\n<li><strong>Non-Verifiable Claims:</strong> An agent asserting that &quot;all tests passed&quot; provides no proof that tests were actually executed against the checked-in commit SHA.</li>\n<li><strong>Missing Execution Context:</strong> Summaries rarely record the precise environment variables, node versions, database states, CLI flags, or mock settings used during execution.</li>\n<li><strong>Selective Omission:</strong> A summary can omit unexecuted suites, skipped assertions, timeouts, or intermediate failures.</li>\n<li><strong>Lack of a Stable Contract:</strong> Free-form summaries lack the schema needed for reliable automated validation.</li>\n</ol>\n<p>Structured, machine-readable <strong>verification evidence receipts</strong> make these claims easier to inspect. They complement source review and runner controls; the receipt format itself does not make an agent safe.</p>\n<h2>General Receipt-Design Principles vs. Current Implementations</h2>\n<p>A verification evidence receipt is a versioned, portable artifact recording the evidence its producer collected, along with missing measurements and trust limitations.</p>\n<p>In general receipt design, an evidence specification defines structured schemas that bind source code identities to execution telemetry. In practical software systems, these fall into two distinct categories:</p>\n<ul>\n<li><strong>Experimental / Ingested Project Receipts:</strong> Ingestion schemas (such as <code>codevetter.project-verification-receipt/v1</code>) that parse raw external runner outputs (Playwright JSON, JUnit XML, LCOV) without mutating local state or asserting full system authority.</li>\n<li><strong>Persisted Native / Local Receipts:</strong> Local execution receipts (such as <code>codevetter.local-check/v1</code>) emitted and stored in local databases during supervised verification passes.</li>\n</ul>\n<p>The sections below propose a general design checklist, not a single shipped CodeVetter schema. The experimental project-receipt analyzer does not execute tests, discover commands, or persist receipts into the desktop database. It cannot supply measurements absent from its input. Hashes identify content; they do not authenticate the producer or make storage immutable.</p>\n<pre><code>┌─────────────────────────────────────────────────────────────────────────┐\n│              VERIFICATION EVIDENCE RECEIPT SCHEMA (v1)                  │\n├─────────────────────────────────────────────────────────────────────────┤\n│ 1. Identity &amp; Scope    │ Base Commit, Patch SHA, Task ID, Runner Profile │\n│ 2. Execution Inventory │ Commands, Exit Codes, Output Hash, Duration    │\n│ 3. Failure Taxonomy    │ Agent Regressions vs. Environment vs. Pre-Exist│\n│ 4. Resource Telemetry  │ Peak RSS, Wall Time, CPU, Egress, Sandbox State │\n│ 5. Explicit Bounds     │ Unverified Requirements, Missing Test Paths     │\n│ 6. Fix Closure Trail   │ Linkage to Initial Failure Receipt &amp; Re-check   │\n└─────────────────────────────────────────────────────────────────────────┘\n</code></pre>\n<p>Below is a detailed breakdown of what each section of a robust evidence receipt specification must contain.</p>\n<h2>1. Identity and Scope Bindings</h2>\n<p>An evidence receipt must be linked unambiguously to a specific source tree and task description. The receipt begins by pinning identity references:</p>\n<ul>\n<li><strong>Repository &amp; Revision Identity:</strong> Absolute Git commit SHA of the base repository, target branch, and exact SHA-256 digest of the agent&#39;s patch file.</li>\n<li><strong>Task Intent Reference:</strong> A unique task identifier, along with a digest or text snapshot of the original prompt and acceptance criteria.</li>\n<li><strong>Environment &amp; System Profile:</strong> OS kernel version, architecture (e.g., <code>darwin-arm64</code>), runtime engine version (e.g., Node.js, Rust), and installed CLI versions.</li>\n<li><strong>Verifier Profile:</strong> The specific verifier name, executable version, and schema version (e.g., <code>codevetter.local-check/v1</code>).</li>\n</ul>\n<p>Checking these identities against the candidate revision helps detect &quot;replay drift&quot;—where an older receipt is attached to a new pull request. Declared identity fields alone do not prove which code the producer executed.</p>\n<h2>2. Execution Records and Inventory</h2>\n<p>The execution section records every command invoked during the verification pass, documenting exact inputs and outputs:</p>\n<ul>\n<li><strong>Command Identity:</strong> The selected command and safe arguments, with credential-bearing values redacted. Record omissions explicitly rather than copying secrets into a receipt.</li>\n<li><strong>Terminal Exit States:</strong> Numeric exit codes, process signal terminations (e.g., <code>SIGKILL</code>), and cancellation flags.</li>\n<li><strong>Bounded Output Captures:</strong> SHA-256 hashes of stdout and stderr logs, accompanied by bounded text excerpts (e.g., capped at 64 KiB) capturing the failure tail.</li>\n<li><strong>Test Inventory Details:</strong> Individual counts for total discovered tests, passed tests, failed tests, skipped tests, and suite durations.</li>\n<li><strong>Stable Failure Signatures:</strong> Normalized, hashed representations of error messages and stack traces, stripping non-deterministic elements like timestamps.</li>\n</ul>\n<h2>3. Failure Taxonomy and Classification</h2>\n<p>A simple pass/fail flag collapses important operational distinctions. A receipt can record the following classifications when baseline and runner evidence support them; otherwise attribution should remain uncertain:</p>\n<ul>\n<li><strong>Agent-Introduced Defect:</strong> The test failed due to a logical error or broken assertion directly within the code written by the agent.</li>\n<li><strong>Side-Effect Regression:</strong> A test outside the immediate scope of the requested task failed after the patch was applied, indicating unexpected breakage.</li>\n<li><strong>Pre-Existing Failure:</strong> The check failed identically on the base commit SHA before the agent&#39;s patch was applied.</li>\n<li><strong>Operational / Environment Failure:</strong> Execution was aborted due to infrastructure conditions, such as missing dependencies or port conflicts.</li>\n<li><strong>Transient Recovery:</strong> A test that failed initially but passed upon immediate re-execution, flagged for flaky behavior tracking.</li>\n</ul>\n<h2>4. Resource, Egress, and Safety Boundaries</h2>\n<p>Executing arbitrary agent code presents security and resource risks. A receipt should describe the controls and observations available to assess those risks, without treating incomplete telemetry as proof of safety:</p>\n<ul>\n<li><strong>Resource Consumption:</strong> Peak process-tree Resident Set Size (RSS memory in MiB), sampled CPU usage, wall-time duration, and process spawn counts.</li>\n<li><strong>Network Egress Evidence:</strong> Observed requests or escapes, collection coverage, and the policy actually enforced. An absent network field is not evidence of zero egress; destination details should be minimized or redacted where sensitive.</li>\n<li><strong>Sandbox State:</strong> The configured isolation boundaries and evidence that enforcement was active. A producer&#39;s declaration alone is not independent attestation.</li>\n</ul>\n<h2>5. Explicit Uncertainty and Limitation Bounds</h2>\n<p>Missing evidence is not proof of success. If an agent modifies an API route that lacks test coverage, the evidence receipt must make that gap explicit.</p>\n<p>The limitations section records:</p>\n<ul>\n<li><strong>Unverified Acceptance Criteria:</strong> User requirements that could not be mapped to an executable check.</li>\n<li><strong>Unexecuted Code Paths:</strong> Files modified by the agent that were not exercised by any executed test runner (supported by LCOV or Cobertura XML reports).</li>\n<li><strong><code>no_confidence</code> States:</strong> Sections where telemetry collection was partial or where test reports lack revision-bound selection or resource measurements.</li>\n</ul>\n<h2>6. Fix Linkage and Closure Trails</h2>\n<p>When an agent run fails verification, a fix cycle begins. A production-grade evidence receipt specification supports receipt chaining to establish an auditable history of resolution:</p>\n<ol>\n<li><strong>Initial Failure Receipt:</strong> Emitted when the agent&#39;s first patch fails an executable check, containing the failing signature and diff.</li>\n<li><strong>Fix Candidate Linkage:</strong> The corrective agent run references the initial failure receipt by its unique SHA-256 bundle identity.</li>\n<li><strong>Targeted Re-Check Execution:</strong> The new verification run re-executes the exact failing check identified in the initial receipt.</li>\n<li><strong>Re-Check Receipt:</strong> Records that the targeted check passed at the new revision and links the retained attempts. Broader regression checks and incomplete coverage remain separate from that result.</li>\n</ol>\n<h2>Next Action for Platform Engineers</h2>\n<p>Incorporate structured evidence receipts into your agent execution infrastructure. Replace informal Markdown summaries with a versioned, schema-validated JSON receipt specification that captures base commit identities, command inventory, failure taxonomy, resource bounds, and explicit limitation boundaries.</p>\n<p>To inspect executable receipt schemas and CLI ingestion tooling, review CodeVetter&#39;s open verification documentation or download the local desktop viewer.</p>',
  },
  '/articles/verifying-ai-code-changes-beyond-code-review': {
    title: 'How to verify AI-generated code changes beyond code review',
    seoTitle: 'Verify AI Code Beyond Review | Executable Verification',
    description:
      'Static AI code review cannot prove software works. Learn how to bind task intent, run executable checks, and capture evidence for AI code changes.',
    path: '/articles/verifying-ai-code-changes-beyond-code-review',
    intent: 'technical how-to for engineers verifying autonomous coding agent pull requests',
    schema: ['Article', 'BreadcrumbList'],
    internalLinks: [
      '/coding-agent-verification',
      '/verify-ai-generated-code',
      '/ai-code-review-vs-verification',
      '/verification-evidence-bundle',
      '/benchmark',
    ],
    markdown:
      "## Moving Beyond Static Opinions in AI Code Verification\n\nLarge language models and autonomous coding agents can generate hundreds of lines of code in seconds. However, engineering velocity breaks when developers are asked to approve pull requests they did not author and cannot easily reason about. The default response across many software organizations has been to insert another AI model as a static code reviewer.\n\nStatic AI code review is valuable for discovering obvious risk patterns, flagging style inconsistencies, or explaining complex diffs. But static review cannot prove that software actually works. An LLM reviewing a diff reads source code as text; it does not execute the program, initialize a database, manage browser state, or process HTTP headers.\n\nA pull request generated by an agent can look clean, include docstrings, pass linter checks, and still fail silently at runtime. Relying solely on static code review to verify autonomous code changes introduces a dangerous gap between plausible-looking source text and verified operational reality. To establish confidence in AI-generated code, software teams must move beyond code review to execution-backed verification.\n\n## The Structural Limits of Code Review for Agent Outputs\n\nTo understand why code review alone is insufficient for AI-generated code, consider how autonomous agents construct changes. When an agent attempts a task—such as updating an authentication flow or modifying a database query—it operates against statistical likelihoods and context windows.\n\nThis creates specific failure modes that static code review routinely misses:\n\n1. **Plausible Lookalike Code:** Agents frequently generate code patterns that mirror surrounding repository style but use invalid assumptions regarding library APIs, internal helper contracts, or async execution timing.\n2. **Unseen Edge-Case Behavior:** A diff modifying an authorization check may look mathematically sound in isolation while failing under specific session states, cookie configurations, or concurrent requests.\n3. **Decoy and Scope Drift:** Agents tasked with fixing a bug can inadvertently edit adjacent, working functions or rewrite working tests to fit a broken implementation.\n4. **Environment-Dependent Mutations:** Changes involving DOM interactions, local storage, network retries, or database transactions cannot be verified by inspecting syntax trees.\n\nWhen a human or an LLM reviews these diffs statically, they evaluate plausibility. But software correctness is not a matter of plausibility; it is a matter of executable behavior under defined constraints.\n\n## The 5-Part Executable Verification Loop\n\nExecution-backed verification bridges the gap between agent intent and operational truth. Rather than asking a model whether a diff looks correct, executable verification subjects the change to deterministic checks and captures reproducible evidence.\n\nThe following is a recommended workflow, not a claim that every CodeVetter receipt path implements every phase. In particular, experimental project-receipt ingestion consumes existing runner evidence; it does not execute checks, discover commands, or persist results into the desktop database.\n\nA verification loop consists of five distinct phases:\n\n```\n[Task Intent] ──> [Agent Change] ──> [Executable Checks] ──> [Captured Evidence] ──> [Measurable Verdict]\n```\n\n### 1. Freeze Task Intent and Scope\nVerification begins by binding the exact requested behavior and its acceptance boundaries. If the original task description is ambiguous, the verifier records that ambiguity explicitly rather than allowing the agent or evaluator to invent a friendly interpretation.\n\n### 2. Bind the Repository Revision and Change\nThe exact base Git commit SHA, the workspace state, and the agent's patch must be pinned together. Verification must run against a clean, pinned revision so that external workspace drift cannot alter the outcome.\n\n### 3. Execute Bounded Behavioral Checks\nThe verifier runs authoritative, deterministic checks against the modified codebase. These checks prioritize repository-owned unit and integration tests, supplemented by focused browser journeys or API calls when existing test suites do not exercise the changed behavior.\n\n### 4. Capture Raw Execution Evidence\nRecord the evidence the runner actually captures: command identity, exit codes, bounded and redacted output, resource measurements, network observations, and failure signatures where available. Mark missing measurements and collection limits explicitly. A command-level memory measurement is not automatically a process-tree measurement, and missing network telemetry does not mean zero egress.\n\n### 5. Render a Measurable Verdict\nUse the chosen verifier's documented status vocabulary. CodeVetter's experimental project-receipt analyzer reports `passed`, `failed`, or `no_confidence` separately for correctness, performance, safety, inventory, and overall status. Missing evidence remains explicit; a passing dimension does not establish that unchecked requirements are safe. Unsupported receipt formats are rejected before analysis.\n\n## Establishing Behavioral Boundaries: Browser State and APIs\n\nCode review evaluates syntax; verification evaluates behavioral boundaries. When verifying agent changes, execution must target the exact interface boundaries exposed to users or system callers.\n\n### Browser Journeys and User Interface State\nFor web applications, subtle frontend regressions—such as lost input focus, incorrect state hydration, broken event handlers, or race conditions during async fetches—frequently pass static diff inspection. Executable verification uses headless browser automation (such as Playwright) to run end-to-end flows against local development servers, capturing DOM states, console errors, and visual evidence.\n\n### API Contracts and Authorization\nWhen an agent updates API endpoints or middleware, verification requires executing actual HTTP requests against isolated local endpoints. Checks must explicitly test both valid payload paths and boundary conditions, such as unauthorized access attempts and malformed inputs.\n\n## Failure Taxonomy: Separating Regressions from Environment Noise\n\nA common flaw in automated verification systems is treating every failed command as an agent bug. If a test fails because a local database port was busy, or because an external API timed out, blaming the coding agent distorts quality metrics.\n\nA useful verification workflow records failure classifications when the evidence supports them. The labels below are design guidance, not proof of causal attribution:\n\n- **Agent Defect / Failure:** The executed check failed explicitly due to broken logic or unhandled exceptions introduced by the agent's change.\n- **Possible Regression:** A previously passing test failed after the change. Check comparable baseline conditions, repeatability, and environment differences before attributing the failure to the patch.\n- **Pre-Existing Failure:** The test was already failing on the base commit prior to the agent's run.\n- **Operational / Environment Failure:** Execution was aborted due to infrastructure conditions, such as missing binaries, port conflicts, or memory limits.\n- **Unverified Requirement:** No authoritative test or runner existed to exercise the requested acceptance criteria.\n\nKeep uncertain attribution explicit. Before/after observations alone cannot rule out environment changes or flaky tests.\n\n## Fix Closure and Re-Check Linkage\n\nWhen verification identifies a failure in an agent's patch, the failure evidence becomes structured feedback for a corrective agent iteration.\n\nTo document a bounded fix and re-check trail:\n\n1. **Preserve the Initial Failure:** The failing run, command, exit code, and failure signature are retained in a persisted evidence record.\n2. **Apply the Corrective Patch:** The agent applies a targeted fix in an isolated, clean workspace.\n3. **Re-Run the Failing Check:** The verifier re-executes the exact test that previously failed to confirm resolution.\n4. **Link Before-and-After Evidence:** Link the original failure to the passing re-check, including revision and environment identities. This establishes the recorded check's outcome, not the absence of every regression. Preserve broader checks and unresolved limitations separately.\n\n## Next Action for Technical Teams\n\nDo not rely on second-model LLM review alone to approve agent pull requests. Evaluate your workflow against the five-part loop: freeze task intent, isolate the revision, run behavioral checks, capture evidence, and inspect failure-to-pass re-checks alongside remaining coverage gaps.\n\nTo examine how local verification functions in practice, download CodeVetter or inspect our public benchmark methodology.",
    html: '<h2>Moving Beyond Static Opinions in AI Code Verification</h2>\n<p>Large language models and autonomous coding agents can generate hundreds of lines of code in seconds. However, engineering velocity breaks when developers are asked to approve pull requests they did not author and cannot easily reason about. The default response across many software organizations has been to insert another AI model as a static code reviewer.</p>\n<p>Static AI code review is valuable for discovering obvious risk patterns, flagging style inconsistencies, or explaining complex diffs. But static review cannot prove that software actually works. An LLM reviewing a diff reads source code as text; it does not execute the program, initialize a database, manage browser state, or process HTTP headers.</p>\n<p>A pull request generated by an agent can look clean, include docstrings, pass linter checks, and still fail silently at runtime. Relying solely on static code review to verify autonomous code changes introduces a dangerous gap between plausible-looking source text and verified operational reality. To establish confidence in AI-generated code, software teams must move beyond code review to execution-backed verification.</p>\n<h2>The Structural Limits of Code Review for Agent Outputs</h2>\n<p>To understand why code review alone is insufficient for AI-generated code, consider how autonomous agents construct changes. When an agent attempts a task—such as updating an authentication flow or modifying a database query—it operates against statistical likelihoods and context windows.</p>\n<p>This creates specific failure modes that static code review routinely misses:</p>\n<ol>\n<li><strong>Plausible Lookalike Code:</strong> Agents frequently generate code patterns that mirror surrounding repository style but use invalid assumptions regarding library APIs, internal helper contracts, or async execution timing.</li>\n<li><strong>Unseen Edge-Case Behavior:</strong> A diff modifying an authorization check may look mathematically sound in isolation while failing under specific session states, cookie configurations, or concurrent requests.</li>\n<li><strong>Decoy and Scope Drift:</strong> Agents tasked with fixing a bug can inadvertently edit adjacent, working functions or rewrite working tests to fit a broken implementation.</li>\n<li><strong>Environment-Dependent Mutations:</strong> Changes involving DOM interactions, local storage, network retries, or database transactions cannot be verified by inspecting syntax trees.</li>\n</ol>\n<p>When a human or an LLM reviews these diffs statically, they evaluate plausibility. But software correctness is not a matter of plausibility; it is a matter of executable behavior under defined constraints.</p>\n<h2>The 5-Part Executable Verification Loop</h2>\n<p>Execution-backed verification bridges the gap between agent intent and operational truth. Rather than asking a model whether a diff looks correct, executable verification subjects the change to deterministic checks and captures reproducible evidence.</p>\n<p>The following is a recommended workflow, not a claim that every CodeVetter receipt path implements every phase. In particular, experimental project-receipt ingestion consumes existing runner evidence; it does not execute checks, discover commands, or persist results into the desktop database.</p>\n<p>A verification loop consists of five distinct phases:</p>\n<pre><code>[Task Intent] ──&gt; [Agent Change] ──&gt; [Executable Checks] ──&gt; [Captured Evidence] ──&gt; [Measurable Verdict]\n</code></pre>\n<h3>1. Freeze Task Intent and Scope</h3>\n<p>Verification begins by binding the exact requested behavior and its acceptance boundaries. If the original task description is ambiguous, the verifier records that ambiguity explicitly rather than allowing the agent or evaluator to invent a friendly interpretation.</p>\n<h3>2. Bind the Repository Revision and Change</h3>\n<p>The exact base Git commit SHA, the workspace state, and the agent&#39;s patch must be pinned together. Verification must run against a clean, pinned revision so that external workspace drift cannot alter the outcome.</p>\n<h3>3. Execute Bounded Behavioral Checks</h3>\n<p>The verifier runs authoritative, deterministic checks against the modified codebase. These checks prioritize repository-owned unit and integration tests, supplemented by focused browser journeys or API calls when existing test suites do not exercise the changed behavior.</p>\n<h3>4. Capture Raw Execution Evidence</h3>\n<p>Record the evidence the runner actually captures: command identity, exit codes, bounded and redacted output, resource measurements, network observations, and failure signatures where available. Mark missing measurements and collection limits explicitly. A command-level memory measurement is not automatically a process-tree measurement, and missing network telemetry does not mean zero egress.</p>\n<h3>5. Render a Measurable Verdict</h3>\n<p>Use the chosen verifier&#39;s documented status vocabulary. CodeVetter&#39;s experimental project-receipt analyzer reports <code>passed</code>, <code>failed</code>, or <code>no_confidence</code> separately for correctness, performance, safety, inventory, and overall status. Missing evidence remains explicit; a passing dimension does not establish that unchecked requirements are safe. Unsupported receipt formats are rejected before analysis.</p>\n<h2>Establishing Behavioral Boundaries: Browser State and APIs</h2>\n<p>Code review evaluates syntax; verification evaluates behavioral boundaries. When verifying agent changes, execution must target the exact interface boundaries exposed to users or system callers.</p>\n<h3>Browser Journeys and User Interface State</h3>\n<p>For web applications, subtle frontend regressions—such as lost input focus, incorrect state hydration, broken event handlers, or race conditions during async fetches—frequently pass static diff inspection. Executable verification uses headless browser automation (such as Playwright) to run end-to-end flows against local development servers, capturing DOM states, console errors, and visual evidence.</p>\n<h3>API Contracts and Authorization</h3>\n<p>When an agent updates API endpoints or middleware, verification requires executing actual HTTP requests against isolated local endpoints. Checks must explicitly test both valid payload paths and boundary conditions, such as unauthorized access attempts and malformed inputs.</p>\n<h2>Failure Taxonomy: Separating Regressions from Environment Noise</h2>\n<p>A common flaw in automated verification systems is treating every failed command as an agent bug. If a test fails because a local database port was busy, or because an external API timed out, blaming the coding agent distorts quality metrics.</p>\n<p>A useful verification workflow records failure classifications when the evidence supports them. The labels below are design guidance, not proof of causal attribution:</p>\n<ul>\n<li><strong>Agent Defect / Failure:</strong> The executed check failed explicitly due to broken logic or unhandled exceptions introduced by the agent&#39;s change.</li>\n<li><strong>Possible Regression:</strong> A previously passing test failed after the change. Check comparable baseline conditions, repeatability, and environment differences before attributing the failure to the patch.</li>\n<li><strong>Pre-Existing Failure:</strong> The test was already failing on the base commit prior to the agent&#39;s run.</li>\n<li><strong>Operational / Environment Failure:</strong> Execution was aborted due to infrastructure conditions, such as missing binaries, port conflicts, or memory limits.</li>\n<li><strong>Unverified Requirement:</strong> No authoritative test or runner existed to exercise the requested acceptance criteria.</li>\n</ul>\n<p>Keep uncertain attribution explicit. Before/after observations alone cannot rule out environment changes or flaky tests.</p>\n<h2>Fix Closure and Re-Check Linkage</h2>\n<p>When verification identifies a failure in an agent&#39;s patch, the failure evidence becomes structured feedback for a corrective agent iteration.</p>\n<p>To document a bounded fix and re-check trail:</p>\n<ol>\n<li><strong>Preserve the Initial Failure:</strong> The failing run, command, exit code, and failure signature are retained in a persisted evidence record.</li>\n<li><strong>Apply the Corrective Patch:</strong> The agent applies a targeted fix in an isolated, clean workspace.</li>\n<li><strong>Re-Run the Failing Check:</strong> The verifier re-executes the exact test that previously failed to confirm resolution.</li>\n<li><strong>Link Before-and-After Evidence:</strong> Link the original failure to the passing re-check, including revision and environment identities. This establishes the recorded check&#39;s outcome, not the absence of every regression. Preserve broader checks and unresolved limitations separately.</li>\n</ol>\n<h2>Next Action for Technical Teams</h2>\n<p>Do not rely on second-model LLM review alone to approve agent pull requests. Evaluate your workflow against the five-part loop: freeze task intent, isolate the revision, run behavioral checks, capture evidence, and inspect failure-to-pass re-checks alongside remaining coverage gaps.</p>\n<p>To examine how local verification functions in practice, download CodeVetter or inspect our public benchmark methodology.</p>',
  },
  '/articles/why-reproducible-evidence-matters-when-an-ai-agent-says-a-task-is-done': {
    title: 'Why reproducible evidence matters when an AI agent says a task is done',
    seoTitle: 'Why Reproducible Evidence Matters for AI Coding Agents | CodeVetter',
    description:
      'When an AI coding agent says a task is done, how do you verify it? Learn why reproducible execution evidence matters more than LLM-as-judge opinions.',
    path: '/articles/why-reproducible-evidence-matters-when-an-ai-agent-says-a-task-is-done',
    intent:
      'informational guide for developers validating autonomous coding agent output beyond manual code review',
    schema: ['Article', 'BreadcrumbList'],
    internalLinks: [
      '/coding-agent-verification',
      '/verify-ai-generated-code',
      '/verification-evidence-bundle',
      '/benchmark',
      '/download',
    ],
    markdown:
      'The adoption of AI coding agents has shifted the bottleneck of software engineering. We are no longer constrained by the speed at which we can draft new features. Instead, the primary constraint has become verification. When an autonomous coding agent completes a task and reports "done," how do we actually know the change is correct?\n\nIn many workflows, the answer is manual inspection. A developer must read the generated diff, pull the branch, run the test suite, and click through the application to ensure nothing broke. This process is time-consuming and negates the velocity gains the agent was supposed to provide.\n\nTo bridge this trust gap, the industry must move beyond trusting an agent\'s self-reported completion and beyond asking a second model for its opinion. We need reproducible execution evidence.\n\n## The Limits of LLM-as-Judge and Static Review\n\nAs the volume of agent-generated code increases, a common response has been to automate the review process using the same technology. This approach involves feeding the diff to another language model and asking it to identify defects. Hosted pull request bots analyze the structure of the change and flag potential issues.\n\nWhile static AI review can catch syntax errors or stylistic inconsistencies, it suffers from a fundamental limitation: it is not executable proof. An LLM reading a diff is providing an opinion based on pattern matching. It does not know if the code will compile. It does not know if a new API endpoint correctly parses the payload.\n\nFurthermore, static review tools lack the broader execution context of the repository. They view the diff in isolation, perhaps with some surrounding file context, but they do not run the application. When an agent hallucinates a variable name, a static reviewer might miss the error if the hallucination looks plausible.\n\nExecutable outcomes outrank model opinion. When the goal is to determine whether a software task was completed correctly, a deterministic test failure is infinitely more valuable than a probabilistic model\'s approval.\n\n## Defining Reproducible Execution Evidence\n\nIf static review is insufficient, what is the alternative? The answer lies in reproducible execution evidence.\n\nExecution evidence is generated by running the code in a deterministic environment and observing its behavior. It moves the verification process from the realm of "looks correct" to "provably works." Reproducible evidence means that any developer, or any other agent, can take the same code, run the same checks, and achieve the exact same results without relying on a hidden cloud state.\n\nIn practical terms, reproducible execution evidence includes:\n\n- **Build Logs:** Does the code compile without errors?\n- **Test Results:** Do the unit and integration tests pass? Did the agent\'s change break an unrelated test?\n- **Runtime Behavior:** For web applications, does the server start? Can a script interact with the newly created API?\n\nBy capturing this evidence deterministically, we create a durable, machine-readable record of the software\'s state. This record serves as the source of truth for whether the agent succeeded.\n\n## The Verification Loop\n\nTo effectively utilize execution evidence, it must be integrated into a tight loop. At CodeVetter, we structure this around a specific process: **task → agent change → executable verification → evidence → measurable verdict**.\n\n1. **Task:** The workflow begins with a clear intent, such as fixing a bug.\n2. **Agent Change:** The coding agent executes the task, modifying the local repository.\n3. **Executable Verification:** Instead of immediately asking a human to review the diff, the system runs deterministic checks. This might involve running a Playwright script.\n4. **Evidence:** The verification step produces concrete artifacts—JSON test reports, build logs, and traces.\n5. **Measurable Verdict:** The evidence is synthesized into a clear outcome. Did the tests pass? Was the bug fixed?\n\nIf the verdict is a failure, the evidence is fed back to the agent as a concrete diagnostic. The agent is given the exact compiler error or the precise test assertion that failed. This deterministic feedback loop allows the agent to iteratively correct its work.\n\n## A Concrete Example: The API Boundary\n\nConsider a web development task: a developer asks an agent to update a backend to support a new query parameter on a search endpoint.\n\nThe agent modifies the controller, adds the logic, and reports the task as complete. A static AI reviewer might look at the diff, confirm that the syntax is valid, and approve the pull request.\n\nHowever, an execution-backed verification system takes a different approach. It runs a local test script that starts the server and sends an HTTP request with the new parameter.\n\nImagine the agent made a subtle error: it parsed the parameter as a string, but the database function expects an integer.\n\nThe static reviewer missed this because it didn\'t trace the type through the application hierarchy. But the executable verification catches it instantly. The request returns a `500 Internal Server Error`, and the test framework captures the stack trace indicating a type mismatch.\n\nThe system captures this execution evidence and provides a measurable verdict: Failure. The developer (or the agent itself) can now see exactly what went wrong, supported by a reproducible runtime trace. There is no ambiguity.\n\n*\n\n## Local, Bounded, and Reversible Workflows\n\nA critical aspect of reproducible evidence is where and how it is generated. For verification to be fast, secure, and iterative, it should be local to the repository.\n\nMany AI coding tools rely on hosted infrastructure—pushing code to a cloud service where a PR bot analyzes the diff. This introduces latency, requires granting third-party access to proprietary source code, and creates a disconnect between the developer\'s local environment and the verification environment.\n\nA local, bounded workflow means the execution evidence is gathered exactly where the agent made the changes: on the developer\'s machine or within an isolated, deterministic local sandbox.\n\n- **Privacy and Security:** Code never leaves the local machine for verification. There is no need to upload proprietary logic to a third-party server just to run a test suite.\n- **Speed:** Local execution eliminates network latency. A developer can ask an agent for a change, and the verification loop can run in seconds, right in the same workspace.\n- **Reversibility:** By keeping the workflow local and bound to the repository\'s version control system, changes are easily reversible. If the executable verification shows a catastrophic failure, the local Git worktree can be reset instantly.\n\nAt CodeVetter, our architecture reflects this principle. The verification engine is a native desktop binary and Rust core that operates over local SQLite. It does not require a hosted server to evaluate whether a local application functions correctly.\n\n*\n\n## Moving from Trust to Verification\n\nThe goal of AI coding agents is not to write code that looks good; it is to write code that works. As long as we rely on manual inspection or non-executable model opinions to bridge the trust gap, we will be bottlenecked by the limits of static analysis.\n\nBy demanding reproducible execution evidence, engineering teams can change their relationship with coding agents. Instead of acting as full-time code reviewers, developers can become orchestrators—setting the tasks, defining the boundaries, and letting deterministic verification handle the proof.\n\nIf you are incorporating autonomous agents into your workflow, the most important question to ask is not "What model is the agent using?" but rather, "How will I execute and verify the result?"\n\n### Practical Next Action\n\nTo start shifting toward execution-backed verification, pick one critical component of your application—such as an authentication flow or a core data transformation function. Write a deterministic, local test script (using a framework like Vitest or Playwright) that exercises this component from end to end.\n\nThe next time you use a coding agent to modify that component, do not review the diff first. Run the script. Let the execution evidence tell you whether the agent succeeded.',
    html: '<p>The adoption of AI coding agents has shifted the bottleneck of software engineering. We are no longer constrained by the speed at which we can draft new features. Instead, the primary constraint has become verification. When an autonomous coding agent completes a task and reports &quot;done,&quot; how do we actually know the change is correct?</p>\n<p>In many workflows, the answer is manual inspection. A developer must read the generated diff, pull the branch, run the test suite, and click through the application to ensure nothing broke. This process is time-consuming and negates the velocity gains the agent was supposed to provide.</p>\n<p>To bridge this trust gap, the industry must move beyond trusting an agent&#39;s self-reported completion and beyond asking a second model for its opinion. We need reproducible execution evidence.</p>\n<h2>The Limits of LLM-as-Judge and Static Review</h2>\n<p>As the volume of agent-generated code increases, a common response has been to automate the review process using the same technology. This approach involves feeding the diff to another language model and asking it to identify defects. Hosted pull request bots analyze the structure of the change and flag potential issues.</p>\n<p>While static AI review can catch syntax errors or stylistic inconsistencies, it suffers from a fundamental limitation: it is not executable proof. An LLM reading a diff is providing an opinion based on pattern matching. It does not know if the code will compile. It does not know if a new API endpoint correctly parses the payload.</p>\n<p>Furthermore, static review tools lack the broader execution context of the repository. They view the diff in isolation, perhaps with some surrounding file context, but they do not run the application. When an agent hallucinates a variable name, a static reviewer might miss the error if the hallucination looks plausible.</p>\n<p>Executable outcomes outrank model opinion. When the goal is to determine whether a software task was completed correctly, a deterministic test failure is infinitely more valuable than a probabilistic model&#39;s approval.</p>\n<h2>Defining Reproducible Execution Evidence</h2>\n<p>If static review is insufficient, what is the alternative? The answer lies in reproducible execution evidence.</p>\n<p>Execution evidence is generated by running the code in a deterministic environment and observing its behavior. It moves the verification process from the realm of &quot;looks correct&quot; to &quot;provably works.&quot; Reproducible evidence means that any developer, or any other agent, can take the same code, run the same checks, and achieve the exact same results without relying on a hidden cloud state.</p>\n<p>In practical terms, reproducible execution evidence includes:</p>\n<ul>\n<li><strong>Build Logs:</strong> Does the code compile without errors?</li>\n<li><strong>Test Results:</strong> Do the unit and integration tests pass? Did the agent&#39;s change break an unrelated test?</li>\n<li><strong>Runtime Behavior:</strong> For web applications, does the server start? Can a script interact with the newly created API?</li>\n</ul>\n<p>By capturing this evidence deterministically, we create a durable, machine-readable record of the software&#39;s state. This record serves as the source of truth for whether the agent succeeded.</p>\n<h2>The Verification Loop</h2>\n<p>To effectively utilize execution evidence, it must be integrated into a tight loop. At CodeVetter, we structure this around a specific process: <strong>task → agent change → executable verification → evidence → measurable verdict</strong>.</p>\n<ol>\n<li><strong>Task:</strong> The workflow begins with a clear intent, such as fixing a bug.</li>\n<li><strong>Agent Change:</strong> The coding agent executes the task, modifying the local repository.</li>\n<li><strong>Executable Verification:</strong> Instead of immediately asking a human to review the diff, the system runs deterministic checks. This might involve running a Playwright script.</li>\n<li><strong>Evidence:</strong> The verification step produces concrete artifacts—JSON test reports, build logs, and traces.</li>\n<li><strong>Measurable Verdict:</strong> The evidence is synthesized into a clear outcome. Did the tests pass? Was the bug fixed?</li>\n</ol>\n<p>If the verdict is a failure, the evidence is fed back to the agent as a concrete diagnostic. The agent is given the exact compiler error or the precise test assertion that failed. This deterministic feedback loop allows the agent to iteratively correct its work.</p>\n<h2>A Concrete Example: The API Boundary</h2>\n<p>Consider a web development task: a developer asks an agent to update a backend to support a new query parameter on a search endpoint.</p>\n<p>The agent modifies the controller, adds the logic, and reports the task as complete. A static AI reviewer might look at the diff, confirm that the syntax is valid, and approve the pull request.</p>\n<p>However, an execution-backed verification system takes a different approach. It runs a local test script that starts the server and sends an HTTP request with the new parameter.</p>\n<p>Imagine the agent made a subtle error: it parsed the parameter as a string, but the database function expects an integer.</p>\n<p>The static reviewer missed this because it didn&#39;t trace the type through the application hierarchy. But the executable verification catches it instantly. The request returns a <code>500 Internal Server Error</code>, and the test framework captures the stack trace indicating a type mismatch.</p>\n<p>The system captures this execution evidence and provides a measurable verdict: Failure. The developer (or the agent itself) can now see exactly what went wrong, supported by a reproducible runtime trace. There is no ambiguity.</p>\n<ul>\n<li></li>\n</ul>\n<h2>Local, Bounded, and Reversible Workflows</h2>\n<p>A critical aspect of reproducible evidence is where and how it is generated. For verification to be fast, secure, and iterative, it should be local to the repository.</p>\n<p>Many AI coding tools rely on hosted infrastructure—pushing code to a cloud service where a PR bot analyzes the diff. This introduces latency, requires granting third-party access to proprietary source code, and creates a disconnect between the developer&#39;s local environment and the verification environment.</p>\n<p>A local, bounded workflow means the execution evidence is gathered exactly where the agent made the changes: on the developer&#39;s machine or within an isolated, deterministic local sandbox.</p>\n<ul>\n<li><strong>Privacy and Security:</strong> Code never leaves the local machine for verification. There is no need to upload proprietary logic to a third-party server just to run a test suite.</li>\n<li><strong>Speed:</strong> Local execution eliminates network latency. A developer can ask an agent for a change, and the verification loop can run in seconds, right in the same workspace.</li>\n<li><strong>Reversibility:</strong> By keeping the workflow local and bound to the repository&#39;s version control system, changes are easily reversible. If the executable verification shows a catastrophic failure, the local Git worktree can be reset instantly.</li>\n</ul>\n<p>At CodeVetter, our architecture reflects this principle. The verification engine is a native desktop binary and Rust core that operates over local SQLite. It does not require a hosted server to evaluate whether a local application functions correctly.</p>\n<ul>\n<li></li>\n</ul>\n<h2>Moving from Trust to Verification</h2>\n<p>The goal of AI coding agents is not to write code that looks good; it is to write code that works. As long as we rely on manual inspection or non-executable model opinions to bridge the trust gap, we will be bottlenecked by the limits of static analysis.</p>\n<p>By demanding reproducible execution evidence, engineering teams can change their relationship with coding agents. Instead of acting as full-time code reviewers, developers can become orchestrators—setting the tasks, defining the boundaries, and letting deterministic verification handle the proof.</p>\n<p>If you are incorporating autonomous agents into your workflow, the most important question to ask is not &quot;What model is the agent using?&quot; but rather, &quot;How will I execute and verify the result?&quot;</p>\n<h3>Practical Next Action</h3>\n<p>To start shifting toward execution-backed verification, pick one critical component of your application—such as an authentication flow or a core data transformation function. Write a deterministic, local test script (using a framework like Vitest or Playwright) that exercises this component from end to end.</p>\n<p>The next time you use a coding agent to modify that component, do not review the diff first. Run the script. Let the execution evidence tell you whether the agent succeeded.</p>',
  },
};
